locals {
  # integration_services_rg_name = "${var.tenant_name}-platform-${var.bu_name}-${var.environment}-integration-services-rg"
  fabric_capacity_name         = "${var.tenant_name}platformccaidevfbrcp${var.location_shortname}01"
  # workspace_name               = "${var.tenant_name}-platform-${var.bu_name}-${var.environment}-fabric-workspace"
}